@if (ViewBag.Title == "Login")
{
    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="@Url.Action("Index", "Admin")">Darbhanga City</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse">
                <form class="navbar-form navbar-right">
                    <div class="form-group">
                        <input type="text" placeholder="Email" class="form-control">
                    </div>
                    <div class="form-group">
                        <input type="password" placeholder="Password" class="form-control">
                    </div>
                    <button type="submit" class="btn btn-success">Sign in</button>
                </form>
            </div>
            <!--/.navbar-collapse -->
        </div>
    </nav>
}
else
{
    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="@Url.Action("Index", "Admin")">Darbhanga City</a>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Select Configuration <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li class="active">@Html.ActionLink("Category", "Index", "CCategory")</li>
                            <li class="active">@Html.ActionLink("Post Category", "Index", "CPostCategory")</li>
                            <li class="active">@Html.ActionLink("Post", "Index", "CPost")</li>
                            <li class="active">@Html.ActionLink("Post Image", "Index", "CPostImageConfig")</li>
                            <li class="active">@Html.ActionLink("Status Condition", "Index", "CStatusCondition")</li>
                            <li class="active">@Html.ActionLink("User Registration", "Index", "CUserRegistration")</li>
                            <li class="active">@Html.ActionLink("Visitor Category", "Index", "CVisitorCategory")</li>
                            <li class="active">@Html.ActionLink("Slider Config", "Index", "CSliderConfig")</li>
                            <li class="active">@Html.ActionLink("Page Content", "Index", "CPageContent")</li>
                            <li class="active">@Html.ActionLink("Logo", "Index", "CLogo")</li>
                            <li class="active">@Html.ActionLink("Role", "Index", "CRole")</li>
                            <li class="active">@Html.ActionLink("User In Role", "Index", "CUserInRole")</li>
                            <li class="active">@Html.ActionLink("Create Menu For Search Your Need", "Index", "CSearchYourNeedLink")</li>
                        </ul>
                    </li>
                    @*<li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Dropdown <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="#">Action</a></li>
            <li><a href="#">Another action</a></li>
            <li><a href="#">Something else here</a></li>
            <li role="separator" class="divider"></li>
            <li><a href="#">Separated link</a></li>
            <li role="separator" class="divider"></li>
            <li><a href="#">One more separated link</a></li>
          </ul>
        </li>*@
                </ul>
                @* <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form>*@
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="@Url.Action("Manage","Account")">
                        @if (Request.IsAuthenticated)
                        {
                            <span>Welcome @User.Identity.Name</span>
                        }

                    </a>
                    </li>
                    <li>
                        @if (Request.IsAuthenticated)
                        {
                            using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
                            {
                            @Html.AntiForgeryToken()
                            <a class="" href="javascript:document.getElementById('logoutForm').submit()" title="Logoff">Logoff
                            </a>
                            }
                        }
                        else
                        {
                            @Html.ActionLink("Login", "Login", "Account") <text>|</text>
                        }
                    </li>
                    @*<li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Dropdown <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="#">Action</a></li>
                            <li><a href="#">Another action</a></li>
                            <li><a href="#">Something else here</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="#">Separated link</a></li>
                        </ul>
                    </li>*@
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container-fluid -->
    </nav>
}